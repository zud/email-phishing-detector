# Requirements for phishing_agent_with_phishtank.py

# Core dependencies
transformers>=4.36.2
sentence-transformers>=2.2.2
torch>=2.0.0

# Email parsing
mail-parser>=3.15.0
extract-msg>=0.45.0

# Environment and arguments
python-dotenv>=1.0.0

# Fuzzy matching for typosquatting
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.12.2  # Optional, speeds up fuzzywuzzy

# URL handling
requests>=2.31.0

# Optional: only needed if using GUI version
# streamlit>=1.31.0
